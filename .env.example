# --- Database Configuration (MySQL) ---
MYSQL_ROOT_PASSWORD=NewStrongPassword123
MYSQL_DATABASE=student_achievement
MYSQL_USER=stuach_user
MYSQL_PASSWORD=stuach_pass

# --- Backend Configuration (NestJS) ---
BACKEND_PORT=5001
NODE_ENV=production
# Update this with the same host name if using Docker networking
DATABASE_URL=mysql://root:NewStrongPassword123@db:3306/student_achievement
JWT_SECRET=689574be96bcb01e8dadb23191f9b441
JWT_EXPIRES_IN=1d

# AWS Configuration (S3)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=YOUR_ACCESS_KEY
AWS_SECRET_ACCESS_KEY=YOUR_SECRET_KEY
AWS_S3_BUCKET=student-achievement-files
STORAGE_MODE=local

# External APIs
GEMINI_API_KEY=YOUR_GEMINI_KEY

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=YOUR_EMAIL
SMTP_PASS=YOUR_APP_PASSWORD
SMTP_FROM_NAME=Vignan Student Portal
SMTP_FROM_EMAIL=YOUR_EMAIL

# --- Frontend Configuration (Next.js) ---
FRONTEND_PORT=3000
# Note: NEXT_PUBLIC_API_URL is used during Docker build as an ARG
